{
  "name": "text-trace-monorepo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @text-trace/core build && pnpm --filter @text-trace/vue build && pnpm --filter @text-trace/react build && pnpm --filter text-trace-playground build",
    "test": "pnpm build && node --test tests/*.test.mjs",
    "bench": "pnpm build && node scripts/bench.mjs",
    "check": "pnpm -r --if-present check",
    "dev": "pnpm --filter text-trace-playground dev",
    "playground": "pnpm --filter text-trace-playground dev",
    "release": "pnpm check && pnpm build && pnpm -r --filter './packages/*' publish --publish-branch main",
    "release:dry-run": "pnpm check && pnpm build && pnpm -r --filter './packages/*' publish --publish-branch main --dry-run --no-git-checks"
  },
  "devDependencies": {
    "@types/opentype.js": "^1.3.9",
    "@types/react": "^19.2.14",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
